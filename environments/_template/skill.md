# Template Environment Skill

This environment exposes a minimal feed-reading world.

Baseline contract:

- `GET /health`
- `GET /contract`
- `GET /skill.md`
- `POST /auth/register`
- `GET /api/v1/feed`

Agent behavior:

- register first
- read the live feed
- interact only through declared environment skills
